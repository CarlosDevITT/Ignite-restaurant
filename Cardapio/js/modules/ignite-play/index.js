// Ignite Play 3.1 — plataforma de minijogos mobile-first e isolada do fluxo de pedidos.
import { getGame, listGames } from './game-registry.js';
import { getHighScore, setHighScoreIfBetter } from './score-store.js';
const STYLE_ID='ignite-play-styles',LOCK_CLASS='ignite-play-lock',SWIPE_THRESHOLD=22;
const KEY_DIRECTIONS={ArrowUp:'up',ArrowDown:'down',ArrowLeft:'left',ArrowRight:'right',w:'up',s:'down',a:'left',d:'right',W:'up',S:'down',A:'left',D:'right'};
const MARKUP=`<div class="ignite-play-overlay" role="dialog" aria-modal="true" aria-labelledby="ignite-play-title"><main class="ignite-play-console"><header class="ignite-play-header"><button type="button" class="ignite-play-iconbtn" data-action="back" aria-label="Voltar">‹</button><div class="ignite-play-heading"><span id="ignite-play-title">IGNITE PLAY</span><small data-role="order-label"></small></div><button type="button" class="ignite-play-iconbtn" data-action="close" aria-label="Fechar">×</button></header><section class="ignite-play-library-view" data-role="library-view"><div class="ignite-play-hero"><span>ARCADE</span><h2>Jogue enquanto espera</h2><p>Seu pedido continua sendo acompanhado em segundo plano.</p></div><div class="ignite-play-library__grid" data-role="game-grid"></div></section><section class="ignite-play-game-view" data-role="game-view" hidden><div class="ignite-play-gamebar"><div><span data-role="game-icon"></span><strong data-role="game-name"></strong></div><span class="ignite-play-live"><i></i><b data-role="state-label">PRONTO</b></span></div><div class="ignite-play-screen"><div class="ignite-play-screen__hud"><div><span>SCORE</span><strong data-role="score">000</strong></div><div><span>RECORDE</span><strong data-role="best">000</strong></div></div><div class="ignite-play-screen__canvas-wrap" data-role="canvas-wrap"><canvas class="ignite-play-canvas" data-role="canvas"></canvas><div class="ignite-play-panel" data-role="panel"></div></div></div><p class="ignite-play-control-hint" data-role="control-hint"></p><div class="ignite-play-controls"><div class="ignite-play-dpad" data-role="dpad" aria-label="Direcionais"><button type="button" data-dir="up" aria-label="Cima">▲</button><button type="button" data-dir="left" aria-label="Esquerda">◀</button><span></span><button type="button" data-dir="right" aria-label="Direita">▶</button><button type="button" data-dir="down" aria-label="Baixo">▼</button></div><div class="ignite-play-actions"><button type="button" data-action="pause">Ⅱ<small>PAUSA</small></button><button type="button" class="is-primary" data-action="a">A<small>JOGAR</small></button></div></div></section></main></div>`;
let overlay=null,activeGame=null,activeMeta=null,currentScore=0,bestScore=0,currentOrderNumber='',onCloseCallback=null,previousFocus=null,keydownHandler=null,pointerStart=null;
const ensureStyles=()=>{if(document.getElementById(STYLE_ID))return;const l=document.createElement('link');l.id=STYLE_ID;l.rel='stylesheet';l.href=new URL('../../../styles/ignite-play.css',import.meta.url).href;document.head.appendChild(l);};
const q=r=>overlay?.querySelector(`[data-role="${r}"]`),formatScore=v=>String(Math.max(0,Math.floor(v))).padStart(3,'0');
const stateLabel=s=>s==='playing'?'JOGANDO':s==='paused'?'PAUSADO':s==='game_over'?'FIM':'PRONTO';
function renderLibrary(){activeGame?.destroy();activeGame=null;activeMeta=null;currentScore=bestScore=0;q('library-view').hidden=false;q('game-view').hidden=true;overlay.dataset.view='library';delete overlay.dataset.controls;overlay.querySelector('[data-action="back"]').hidden=true;q('game-grid').innerHTML=listGames().map(g=>`<button type="button" class="ignite-play-game-card" data-game-id="${g.id}"><span class="ignite-play-game-card__art">${g.icon}</span><span class="ignite-play-game-card__body"><strong>${g.name}</strong><small>${g.description}</small><em>${g.controls}</em></span><span class="ignite-play-game-card__meta"><small>HI</small><b>${formatScore(getHighScore(g.id))}</b><i>›</i></span></button>`).join('');requestAnimationFrame(()=>overlay?.querySelector('[data-game-id]')?.focus());}
function panelMarkup(s){if(s==='ready')return `<span class="ignite-play-panel__icon">${activeMeta.icon}</span><strong>${activeMeta.name}</strong><p>${activeMeta.description}</p><button data-action="a">JOGAR AGORA</button>`;if(s==='paused')return `<strong>PAUSADO</strong><p>Seu pedido continua sendo acompanhado.</p><button data-action="pause">CONTINUAR</button>`;if(s==='game_over')return `<strong>GAME OVER</strong><div class="ignite-play-panel__scores"><span>SCORE <b>${formatScore(currentScore)}</b></span><span>RECORDE <b>${formatScore(bestScore)}</b></span></div><button data-action="a">JOGAR DE NOVO</button><button class="is-secondary" data-action="back">OUTROS JOGOS</button>`;return'';}
function applyState(s){if(!activeMeta)return;if(s==='game_over'){bestScore=setHighScoreIfBetter(activeMeta.id,currentScore);q('best').textContent=formatScore(bestScore);}const p=q('panel');p.innerHTML=panelMarkup(s);p.classList.toggle('is-visible',s!=='playing');q('state-label').textContent=stateLabel(s);overlay.querySelector('.ignite-play-live').classList.toggle('is-active',s==='playing');overlay.dataset.state=s;}
function launchGame(id){const m=getGame(id);if(!m)return;activeGame?.destroy();activeMeta=m;currentScore=0;bestScore=getHighScore(m.id);q('library-view').hidden=true;q('game-view').hidden=false;overlay.dataset.view='game';overlay.dataset.controls=m.controlMode||'all';overlay.querySelector('[data-action="back"]').hidden=false;q('game-icon').textContent=m.icon;q('game-name').textContent=m.name;q('control-hint').textContent=m.controls;q('score').textContent='000';q('best').textContent=formatScore(bestScore);activeGame=m.create({canvas:q('canvas'),onScoreChange:v=>{currentScore=v;q('score').textContent=formatScore(v);},onStateChange:applyState});applyState(activeGame.getState());}
function handleStart(){if(!activeGame)return;const s=activeGame.getState();s==='playing'?activeGame.pause():s==='paused'?activeGame.resume():activeGame.start();}function handleA(){if(!activeGame)return;const s=activeGame.getState();s==='paused'?activeGame.resume():s!=='playing'&&activeGame.start();}
function wire(){overlay.addEventListener('click',e=>{const g=e.target.closest('[data-game-id]');if(g)return launchGame(g.dataset.gameId);const a=e.target.closest('[data-action]');if(!a)return;if(a.dataset.action==='close')IgnitePlay.hide();else if(a.dataset.action==='back')renderLibrary();else if(a.dataset.action==='pause')handleStart();else if(a.dataset.action==='a')handleA();});overlay.querySelectorAll('[data-dir]').forEach(btn=>{btn.addEventListener('pointerdown',e=>{e.preventDefault();btn.setPointerCapture?.(e.pointerId);activeGame?.setDirection(btn.dataset.dir);});const release=()=>{if(activeMeta?.controlMode==='vertical'||activeMeta?.controlMode==='horizontal')activeGame?.setDirection('stop');};btn.addEventListener('pointerup',release);btn.addEventListener('pointercancel',release);btn.addEventListener('lostpointercapture',release);});keydownHandler=e=>{const d=KEY_DIRECTIONS[e.key];if(d&&activeGame){e.preventDefault();activeGame.setDirection(d);return;}if((e.key==='Enter'||e.key===' ')&&activeGame){e.preventDefault();handleStart();return;}if(e.key==='Escape'){e.preventDefault();activeMeta?renderLibrary():IgnitePlay.hide();}};document.addEventListener('keydown',keydownHandler);const stopKeyboard=e=>{if(KEY_DIRECTIONS[e.key]&&(activeMeta?.controlMode==='vertical'||activeMeta?.controlMode==='horizontal'))activeGame?.setDirection('stop');};document.addEventListener('keyup',stopKeyboard);keydownHandler._keyup=stopKeyboard;const w=q('canvas-wrap');w.addEventListener('pointerdown',e=>pointerStart={x:e.clientX,y:e.clientY,id:e.pointerId});w.addEventListener('pointerup',e=>{if(!pointerStart||e.pointerId!==pointerStart.id){pointerStart=null;return;}const dx=e.clientX-pointerStart.x,dy=e.clientY-pointerStart.y;pointerStart=null;if(Math.max(Math.abs(dx),Math.abs(dy))<SWIPE_THRESHOLD)return;activeGame?.setDirection(Math.abs(dx)>Math.abs(dy)?(dx>0?'right':'left'):(dy>0?'down':'up'));});}
function unwire(){if(keydownHandler){document.removeEventListener('keydown',keydownHandler);if(keydownHandler._keyup)document.removeEventListener('keyup',keydownHandler._keyup);}keydownHandler=null;pointerStart=null;}
export const IgnitePlay={show(o={}){const{orderId=null,orderNumber='',onClose,gameId=null}=o;if(overlay){overlay.dataset.orderId=orderId??'';currentOrderNumber=orderNumber||currentOrderNumber;q('order-label').textContent=currentOrderNumber?`Pedido #${currentOrderNumber}`:'';if(gameId)launchGame(gameId);return;}ensureStyles();previousFocus=document.activeElement;onCloseCallback=typeof onClose==='function'?onClose:null;currentOrderNumber=orderNumber?String(orderNumber):'';const w=document.createElement('div');w.innerHTML=MARKUP;overlay=w.firstElementChild;overlay.dataset.orderId=orderId??'';document.documentElement.classList.add(LOCK_CLASS);document.body.classList.add(LOCK_CLASS);document.body.appendChild(overlay);q('order-label').textContent=currentOrderNumber?`Pedido #${currentOrderNumber}`:'';wire();gameId&&getGame(gameId)?launchGame(gameId):renderLibrary();},hide(){if(!overlay)return;const cb=onCloseCallback;activeGame?.destroy();unwire();overlay.remove();overlay=null;activeGame=activeMeta=null;document.documentElement.classList.remove(LOCK_CLASS);document.body.classList.remove(LOCK_CLASS);if(previousFocus instanceof HTMLElement)previousFocus.focus();previousFocus=null;onCloseCallback=null;cb?.();},destroy(){this.hide();},isOpen(){return!!overlay;},getScore(){return currentScore;},getActiveGame(){return activeMeta?.id||null;},listGames};export default IgnitePlay;
